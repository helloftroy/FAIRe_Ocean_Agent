"""Text fact extraction (Milestone 4): evidence.py (deterministic
evidence-quote verification), sections.py (deterministic section
selection), retrieval.py (open-access full-text fetch), text.py (the LLM
extraction contract). Structured (API/XML) extraction lives in each
sources/*.py adapter's extract_structured_facts(), not here."""
