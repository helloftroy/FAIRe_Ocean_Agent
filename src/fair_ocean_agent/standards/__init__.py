"""Compiles the vendored FAIRe (schemas/faire/), MIOP (schemas/miop/), and
BeBOP protocol template (schemas/bebop/) schemas into a normalized,
provenance-tracked registry -- see registry.py's docstring for the full
design and `fair-ocean build-standards-registry`.
"""
